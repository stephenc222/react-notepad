import React from 'react';
import ReactDOM from 'react-dom';
import { shallow, mount } from 'enzyme'
import sinon from 'sinon'
import OpenFileBox from '.';

describe.skip('OpenFileBox Component', () => {
  it('allows its\' props to be set', () => {
    // const wrapper = shallow(<UndoStackView undoStackObject={[]}/>)
    // expect(wrapper.find('.undoStackView').length).to.equal(1)
  })
})